language: generic
sudo: required
services: 
  - docker
  
before_install:
  - docker build -t dappilik/docker-react -f Dockerfile.dev .  
  
script:
  - docker run -e CI=true dappilik/docker-react yarn test --coverage